<UserControl x:Class="DOCUMAT.Outils.RadialProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:id="http://schemas.microsoft.com/expression/2010/drawing" 
             xmlns:local="clr-namespace:DOCUMAT.Outils"
             mc:Ignorable="d" 
             x:Name="_this"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!--<Ellipse x:Name="SecondCircle" Stroke="{Binding ElementName=_this,Path=BackgroundBorderColor}" 
                 StrokeThickness="{Binding ElementName=_this,Path=BackgroundBorderThickness}" 
                 Opacity="{Binding ElementName=_this,Path=BackgroundOpacity}"/>
        
        <id:Arc Name="indicator" 
                ArcThickness="{Binding ElementName=_this,Path=indicatorThickness}" ArcThicknessUnit="Pixel" 
                EndAngle="{Binding Converter={StaticResource ValueToAngle},ElementName=_this,Path=Value}"
                Fill="Blue" Stretch="None" StartAngle="0" />
        <TextBlock Text="{Binding ElementName=_this, Path=TextIndicator}"
                   Foreground="White" FontSize="50" FontFamily="Agency Fb" VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>-->
    </Grid>
</UserControl>
